using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Destructoid : CreepMain {

    //TempSolution
    //should be in MapTileScript. OR change mapTileScript to objects. But if we have tiles - then tiles!
    //When I spawn Rocks -> make map tile know that this tile is rocked!

    //and remove colider after cleanup
    //or. let's just make it right =)
	
}
